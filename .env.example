# Public environment variables (exposed to client)
# Copy to .env and fill in values

# Production site URL (used for canonical URLs, sitemap, OG tags)
# Example: https://revanza.dev or https://revanzart.vercel.app
PUBLIC_SITE_URL=